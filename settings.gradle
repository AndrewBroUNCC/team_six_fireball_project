pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //this is for the charts as well
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "team_six_fireball_project"
include ':app'
